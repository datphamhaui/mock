/*
*
* {{ properCase containerName }} Selector
*
*/
import { createSelector } from '@reduxjs/toolkit';

import { RootState } from 'types/RootState';
import { initialState } from '.';

const selectSlice = (state: RootState) => state.{{ camelCase containerName }} || initialState;

export const select{{ properCase containerName }} = createSelector(
  selectSlice,
  state => state,
);
